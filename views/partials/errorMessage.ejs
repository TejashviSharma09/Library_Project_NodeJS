<!-- Partial to display the error message. -->
<!-- We only need to display this if there is a error message so we perform an 'if' check -->

<% if (locals.errorMessage != null) { %>
    <%= errorMessage %>    
<% } %>


